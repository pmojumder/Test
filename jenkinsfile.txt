pipeline {
    agent any  // This specifies that Jenkins can run the pipeline on any available agent

    environment {
        // Optional: Define environment variables here if needed
        // EXAMPLE_VAR = 'value'
    }

    stages {
        stage('Checkout') {
            steps {
                // Checkout the code from your repository
                git 'https://github.com/your-username/your-repo.git'
            }
        }

        stage('Build') {
            steps {
                // Build your project (replace this with the actual build command for your project)
                echo 'Building the project...'
                // Example for Maven:
                // sh 'mvn clean install'
                // Example for Node.js:
                // sh 'npm install'
            }
        }

        stage('Test') {
            steps {
                // Run tests (replace this with the actual test command for your project)
                echo 'Running tests...'
                // Example for Maven:
                // sh 'mvn test'
                // Example for Node.js:
                // sh 'npm test'
            }
        }

        stage('Deploy') {
            steps {
                // Deploy your project (replace this with the actual deploy command for your project)
                echo 'Deploying the project...'
                // Example for deploying to a server:
                // sh 'scp target/*.jar user@server:/deploy/dir'
            }
        }
    }

    post {
        always {
            // Clean up or perform any post-build actions here (optional)
            echo 'Cleaning up...'
        }
        success {
            echo 'Pipeline was successful!'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}
